[tool.ruff]
select = ["F", "E9", "I"]
target-version = "py312"
exclude = [
    "PHASE3/testrepo*",
    "PHASE3/artifacts",
    "dashboard",
    "design-lab"
]

[tool.ruff.per-file-ignores]
"interface/api.py" = ["F401"]  # Ignore import unused for interface/api.py:60 if it's a duplicate of :611
