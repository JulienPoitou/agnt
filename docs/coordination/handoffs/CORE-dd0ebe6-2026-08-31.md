# HANDOFF — CORE-HAPPY-PATH — 2026-08-31

> Enregistré par l'orchestrateur (session `arena/01a05944-agnt`).
> Handoff reçu via rapport, livraison vérifiée au **niveau minimal uniquement** (branche, SHA, périmètre, linéarité). Pas d'audit du code — règle NO AUDIT LOOP.

## Décision orchestrateur

```
STATUS: DONE
AGENT: CORE-HAPPY-PATH
BRANCH: arena/01a05923-agnt (branche de session Arena dérivée de core-console-v0 @ 89bd3b1)
SHA: dd0ebe661c31910d9d6e8fd37b31a4679d1b3527
LIVRABLE: prérequis sandbox conditionnels au provider réellement exécuté (besoins() / verifie() / commande())
VALIDATION: SHA poussé ✓ ; commit dd0ebe6 = exactement les 2 fichiers déclarés ✓ ; historique linéaire depuis 89bd3b1, 0 commit de merge ✓ ; périmètre PHASE3 respecté ✓
BLOCKERS: aucun — murs restants (bwrap, points de montage mt-*, outils) transférés au chantier DEVOPS-LAUNCH
NEXT: pas de relance CORE. Prochain mur = bootstrap/lancement → DEVOPS-LAUNCH.
```

## Correction de registre (importante)

Le garde de branche CORE affirmait « `origin/core-console-v0` n'existe pas côté remote ».
**FAUX** : `core-console-v0` existe à `89bd3b1` (vérifié par l'orchestrateur le 2026-08-31 via `git ls-remote --heads origin`).
C'est le faux négatif du piège refspec documenté dans `ARENA_AGENT_LAUNCH_PLAYBOOK.md` §4 (clone Arena limité au suivi de `main` → `git log origin/<branche>` échoue même si la branche existe).
La conclusion du garde (« cas autorisé → je continue ») restait correcte ; seule la prémisse était fausse. Aucun impact sur le livrable.

## Contexte de la branche

`arena/01a05923-agnt` contient 2 commits depuis la base `89bd3b1` :

- `53e9573` — core: add console-ready mission summary projection for owner dogfooding (`mission_history.py`, `mission_resume.py`, `test_mission_resume.py`) — livraison CORE précédente ;
- `dd0ebe6` — core: make sandbox prerequisites conditional to the executed provider (`sandbox.py`, `test_sandbox_prerequis.py`) — objet du présent handoff.

## Handoff reçu (verbatim)

```text
=== AGNT HANDOFF v1 ===
STATUS: DONE
AGENT: CORE-HAPPY-PATH
BRANCH: arena/01a05923-agnt (branche de session Arena dérivée de core-console-v0 @ 89bd3b1 ; `origin/core-console-v0` inexistant côté remote, HEAD descend de origin/main 89bd3b1)
SHA: dd0ebe661c31910d9d6e8fd37b31a4679d1b3527
FILES MODIFIED:
- PHASE3/slice/sandbox.py (constante `RESSOURCES_OPTIONNELLES`, nouvelle méthode `besoins()`, `verifie(besoins=None)`, `commande(argv, besoins=None)`, `exec()` qui dérive et transmet le besoin)
- PHASE3/test_sandbox_prerequis.py (nouveau, 14 cas)
LIVRABLE: prérequis sandbox conditionnels au provider réellement exécuté. `besoins(argv, env)` rend l'ensemble des ressources optionnelles citées (`regles`, `db`) ; `verifie()` et `commande()` n'exigent et ne montent qu'elles. Le dépôt, la sortie et le gitconfig restent structurels. bandit / ruff / detect-secrets / gitleaks (aucun `{DB}` ni `{REGLES}`) ne sont plus bloqués par une base Trivy absente ; trivy (`--cache-dir={DB}/trivy`), grype (`GRYPE_DB_CACHE_DIR={DB}/grype`, donc par l'ENV), semgrep et kics (`{REGLES}/…`) restent refusés explicitement, avant tout Popen, avec le message d'origine. `verifie()` sans argument reste strict (diagnostic complet, `harnais.py`) et `commande(argv)` sans besoins garde les 5 montages historiques — l'autorité `conditions.egress_de` est intacte. La barrière `conditions.manquantes` (bases déclarées) est inchangée : rien n'est adouci.
VALIDATION:
- `python3 -m compileall -q PHASE3` → OK
- `python3 PHASE3/test_sandbox_prerequis.py` → 14 OK, 0 ECHEC (sans base ni règles = passe jusqu'au Popen ; base citée en argv = refus ; base citée en env = refus ; règles citées = refus, sans réclamer la base ; binaire absent = `outil introuvable` inchangé ; refus avant tout Popen prouvé par témoin ; base présente = exigée ET montée ; montage bootstrap absent et `bwrap` absent restent visibles comme murs réels ; rétro-compat `verifie()`/`commande()`/`egress_de`)
- `git diff --check` → propre
- Non-régression, sorties comparées octet à octet avant/après le patch : `test_conditions_outils.py`, `test_qualite_plateforme.py`, `test_utilisation.py` (24/29, échecs pré-existants dus à l'absence d'outils/opa) → IDENTIQUES ; `test_env_outil.py` 9/9, `test_empreintes.py` 13/13, `test_fanout.py` 10/10 (+3 non évalués) → verts
BLOCKERS: none. Sur cette machine le prochain mur est réel et attendu : `bwrap`, les points de montage `mt-*` (bootstrap.sh) et l'absence totale d'outils — hors périmètre, non touchés.
NEXT: sur une machine bootstrapée, un run bandit/pip-tools doit maintenant atteindre l'exécution sans base Trivy ; le prochain obstacle à traiter sera `bwrap`/montages (chantier bootstrap/DevOps, pas CORE).
=== END AGNT HANDOFF ===
```
