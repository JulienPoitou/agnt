=== AGNT HANDOFF v1 ===

**Agent :** CORE
**Domaine :** moteur — projection History/Timeline/Status, captures de gate, contrat transport
**Branche de session :** `arena/01a0575c-agnt` · tip `1dc6bfec3bcbd0463b3e80c496cfeacaa09df377`
**Base :** `main` `e7262f9` (mergé en fast-forward : la ligne CORE `3aeb8bc` y était déjà intégrée via PR #6)
**PR :** #8 → `main` (OUVERTE, 3 commits)
**Statut :** `READY_FOR_REVIEW` — GATE-002 **CONFIRMÉ par exécution**, TIMELINE-002 fait, MCP-004 répondu
**Date :** 2026-08-31

---

## 1. Commits (3)

| SHA | Contenu |
|---|---|
| `08b6446` | core: project recorded provenance, express interrupted execution, register the availability journal event |
| `e094909` | core: publish controlled full-coverage captures for the product API gate |
| `1dc6bfe` | core: state the canonical transport contract for the MCP raccord |

Périmètre : **23 fichiers, +4153/−4** (`git diff --stat origin/main..HEAD`).
Code touché : `slice/mission_history.py`, `slice/transports.py` (docstring contrat uniquement),
`test_mission_history_api.py`, + `PHASE3/produire_captures_product.py` (nouveau).
Le reste = captures publiées + leur README.

## 2. GATE-002 — captures contrôlées pleine couverture (objectif 1)

**Livrable :** `docs/coordination/captures/gate-002-product-api/` (manifeste + 17 corps de
réponse + README) et son producteur `PHASE3/produire_captures_product.py`.

Le producteur construit 11 missions contrôlées, sert l'API CORE **réelle** en process et
capture ses réponses HTTP réelles. Aucune réponse n'est écrite à la main. La construction des
missions **importe les helpers de `test_mission_history_api.py`** : la capture et la batterie
partagent la même définition d'une mission contrôlée (pas de deuxième vérité).

**Gate Product rejoué** (`docs/coordination/api-conformance-gate/product_api_gate.py`,
extrait de `arena/01a05425-agnt`, exécuté en lecture seule depuis `/tmp` — aucun code PRODUCT
copié dans cette branche) :

```
--capture …/capture-manifest.json --require-full-coverage
AGNT PRODUCT API GATE: 1467 PASS · 0 FAIL · 0 SKIP      → exit 0
couverture exercée : 16 / 16
```

Les 16 cas, vérifiés un par un en instrumentant `Report.coverage` (pas seulement le code de
sortie) : `cancelled`, `detail`, `empty_list`, `failed`, `findings`, `incomplete`, `list`,
`mcp`, `non_applicable`, `refused`, `submission_distinct`, `timeline`, `timeout`,
`unavailable`, `unknown`, `zero`.

**Le gate a attrapé deux incohérences de mes propres fixtures** avant de passer au vert
(exécution sans plan consigné → provider « hors plan » donc `authorization: non_evalue`
tout en publiant un compte de findings). Les captures publiées sont cohérentes de bout en
bout. C'est un signal utile sur le gate, pas un défaut CORE.

## 3. Trois capacités CORE ajoutées — nécessaires pour atteindre la matrice

Sans elles, `--require-full-coverage` sortait en 2 : la matrice était **inatteignable** par
l'API réelle.

1. **Provenance projetée** (`mission_history._provenance`) — allowlist des champs, grammaire
   des identifiants, `provider_kind` **jamais deviné**, champs hostiles jetés. Preuve (cas 20) :
   la fixture consigne `endpoint`, `token: Bearer …`, `argv`, `server_id_brut` → aucun
   n'apparaît dans la réponse ; `provider_kind: mcp`, `transport: stdio`,
   `protocol {name, version}`, `confidence {level, basis}` sont projetés intacts.
   Sans fait consigné → **aucune** provenance (pas de « local » par défaut, qui serait une
   affirmation de confiance non prouvée). Conforme à « MCP détient les faits, CORE projette ».
2. **`cancelled`** — une mission **close** dont le ledger dit encore « en cours » a été
   interrompue. Annoncer `en_cours` sur une mission close était un mensonge observable.
   Dérivation uniquement : **aucun nouveau statut** dans le vocabulaire fermé de `statuts.py`
   (invariant « AUCUN statut n'est saisi », `AssertionError` hors vocabulaire). Aucun finding
   n'en est déduit : `detection: non_evalue`, pas de `findings_count`.
3. **`disponibilite`** (TIMELINE-002) — écrit par `pipeline.py:523` depuis PR #2 mais absent du
   `_REGISTRE` du lecteur : un fait consigné était projeté en `unknown_event_recorded`, donc le
   lecteur affirmait « inconnu » pour un événement connu. Aligné sur le kind du contrat déjà
   utilisé pour `applicabilite` (`providers_filtered` / `plan` / `skipped` / `mission`) —
   vocabulaire du contrat uniquement, **aucun kind inventé**, aucune forme modifiée.

## 4. TIMELINE-002 — réponse précise sur les noms source (objectif 2)

Mesuré sur un journal réel produit par `pipeline.executer` :
`['ouverture', 'confiance', 'egress', 'intention', 'applicabilite', 'conditions', 'plan',
'arret', 'statuts']` (+ `disponibilite`, `contexte`, `execution`, `escalade`, `cloture`
selon le chemin).

- `intention` → `intent_resolved` : **déjà aligné**, rien à changer.
- `disponibilite` → corrigé (point 3 ci-dessus).
- **`providers_selected` n'a pas de nom source CORE.** Le contrat Timeline laisse la ligne
  « provider selection event » ouverte et dit explicitement que les événements CORE
  d'intention/sélection « must be mapped by their actual stable source kind ». Or CORE ne
  consigne **pas** d'événement de sélection distinct : la sélection (choisis / écartés / motif
  par capacité) est portée par l'événement `plan`, déjà mappé en `plan_created`, et projetée
  dans `data.plan.selection`. Ajouter un événement `selection` serait un **changement de forme**
  (exclu par le brief) ; inventer un kind serait un **dialecte concurrent** (interdit).
  → **Question PRODUCT**, pas une décision CORE unilatérale : soit la ligne
  `providers_selected` reste non exercée par CORE (la sélection est lisible via `plan_created`
  + `data.plan.selection`), soit le contrat admet que `plan` porte les deux.

## 5. SUPPORT MCP-004 (objectif 3)

`PHASE3/STATUT_MCP.md` (`451de79`) porte une demande explicite : « comparer les signatures et
exceptions du registre canonique, brancher `mcp_bootstrap` sur son instance/API réelle ».
Réponse écrite **dans le module CORE canonique** (`slice/transports.py`, docstring) — le point
de lecture naturel de MCP. Aucun code de la branche MCP touché.

Points tranchés : `deleguer` appelle `executeur(prov, sbx)` avec **deux arguments positionnels**
(donc les mot-clés à défaut de MCP, `*, target=None, arguments=None, transport_factory=None`,
sont compatibles tels quels) ; **une seule exception** `TransportError` (des types MCP doivent
en hériter pour être attrapés par `adapters`) ; `sandbox_cli` **réservé** ; `connus()` **inclut**
`sandbox_cli` ; **aucun repli** sous-processus ; ré-enregistrement **écrase** (bootstrap
idempotent : `enregistrer("mcp", …)` sans condition, test de présence via `fournit("mcp")`).
Table de correspondance `obtenir/enregistre/noms/UnknownTransport/DuplicateTransport` →
`deleguer/fournit/connus/TransportError` incluse.

## 6. Tests — classification

Environnement : Python 3.11.2, venv `/home/user/.venv-agnt` (pyyaml 6.0.3 ; le Python système
est PEP 668 *externally-managed*). `opa` **absent**, `bwrap` **absent**.

| Suite | Classement | Résultat |
|---|---|---|
| `test_mission_history_api.py` | **PASS** | exit 0 · `36/36` (33 + cas 18 `disponibilite`, 19 `cancelled`, 20 provenance) |
| `test_transports.py` | **PASS** | exit 0 · `12/12` |
| `test_observabilite.py` | **PASS** | exit 0 · `7/7` |
| `test_isolation_mission.py` | **PASS** | exit 0 · `8/8` |
| `test_multi_mission.py` | **PASS** | exit 0 · `11/11` |
| `test_cibles.py` | **PASS** | exit 0 · `33/33` |
| `test_interface.py` | **PASS** | exit 0 · `34/35 · 0 échec · 1 non évalué` |
| `test_slice.py` | **BLOCKED** | exit 1 · `PolicyError: binaire OPA introuvable` (avant tout cas) |
| `test_bundle.py` | **BLOCKED** | exit 1 · « aucun bundle produit » |
| gate Product sur captures | **PASS** | `1467 PASS · 0 FAIL · 0 SKIP` · exit 0 · 16/16 |

Les 2 BLOCKED sont environnementaux et **identiques à la base** (vérifié au tour précédent par
worktree détaché sur `4433af6`) ; aucune non-régression mesurable par ces suites.

**NON ÉVALUÉ :** policy OPA réelle et sandbox réelle dans leur ensemble. Installation d'OPA
retentée et échouée dans ce sandbox : `openpolicyagent.org` → `curl (35) SSL_ERROR_SYSCALL`,
miroir `release-assets.githubusercontent.com` → idem, `pip index versions opa` →
`No matching distribution found` (égress limité à pypi.org / github.com, 200 tous deux).
Aucun fichier partiel laissé.

## 7. Limites

- **Cas `mcp` = projection seulement.** Aucun serveur MCP réel contacté, aucun transport MCP
  enregistré dans cet arbre. Le *producteur* du fait de provenance est MCP-004. Documenté dans
  le README des captures. La projection est vérifiée ; l'acquisition du fait ne l'est pas ici.
- **Pas de `pagination_next`** dans la capture : 11 missions ≤ limite 25, donc pas de
  `next_cursor`. Le cas n'est pas dans `FULL_COVERAGE` et la pagination est couverte par
  `test_mission_history_api.py`.
- `disponibilite` → `providers_filtered` est une **décision de mapping CORE** sur un kind déjà
  existant du contrat ; à faire ratifier par PRODUCT (aucun kind nouveau n'est introduit).
- `providers_selected` non exercé par CORE : voir §4, décision PRODUCT attendue.
- Le gate Product a été exécuté depuis une copie en lecture seule de `arena/01a05425-agnt`
  (PRODUCT n'est pas dans `main`). À rejouer sur l'arbre intégré une fois PRODUCT mergé.

## 8. Périmètre non touché

Aucun code MCP, WEB, SECURITY, PRODUCT. **CORE-005 non ouvert** (interdit sans trilogue
CORE+MCP+SECURITY). Aucun contrat HTTP public modifié (`/api/missions`, `/api/missions/{id}`
inchangés ; `provenance` est additif). Aucun garde-fou affaibli : la projection reste
allowlist + redaction, `provider_kind` n'est jamais deviné, aucune absence ne devient zéro.

## 9. Confiance

- **Haute** sur GATE-002 : sorties du gate lues, couverture 16/16 vérifiée par instrumentation,
  captures produites par l'API réelle et reproductibles.
- **Haute** sur les 3 capacités CORE : chacune a un cas dédié dans une batterie qui passe
  (18, 19, 20), et les 6 autres suites restent vertes.
- **Nulle** sur OPA/sandbox réels : non exécutés ici.

=== END AGNT HANDOFF ===
