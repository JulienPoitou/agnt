# Proposition Web — SourceRipping — git-scanner
# Source: awesome-pentest enaqx/SourceRipping
# URL: https://github.com/HightechSec/git-scanner
# Généré auto depuis docs/WEB_PROVIDERS_CATALOG.md — 119 outils
# Statut: PROPOSITION (non chargé par plugins.py tant que dans propositions/)
id: git_scanner
capacites:
  - WEB_SOURCE
entrees:
  - url
  - hote
binaire: git_scanner
outillage: git_scanner
version_min: "0.1.0"
licence: MIT
priorite: 300
cout: moyen
risque: PASSIVE
description: >
  .git hunting — git-scanner (https://github.com/HightechSec/git-scanner). Proposition web AGNT V1.
  Catégorie SourceRipping → WEB_SOURCE. Voir WEB_PROVIDERS_CATALOG.md.
fichiers_requis: []
execution:
  commande: ["{BIN}/git_scanner"]
  args: ["--help"]
  code_succes: [0, 1]
sortie:
  format: json
lecture:
  modele: plat
  champs:
    regle: null
    message: null
requirements:
  reseau: true
  privileges: aucun
  base_fichiers: []
  timeout_s: 300
  sandbox: true
