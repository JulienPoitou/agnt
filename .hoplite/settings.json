{
  "version": 1,
  "ports": {"preview": 3000, "additional": {}},
  "scripts": {
    "setup": {"enabled": true, "command": "command -v git-lfs >/dev/null 2>&1 || { sudo -n apt-get install -y git-lfs 2>/dev/null || apt-get install -y git-lfs; }; npm install --no-audit --no-fund"},
    "run": {"enabled": true, "command": "PORT=3000 npm run dev"},
    "archive": {"enabled": true, "command": null},
    "check": {"enabled": true, "command": null}
  },
  "mcpServers": []
}
